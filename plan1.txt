# Clean Code Refactoring Plan: class-cdw-ai-service.php

## Overview

This plan refactors `/home/toni/Escritorio/Wordpress/wp-content/plugins/cdw/includes/services/class-cdw-ai-service.php` to follow clean code principles (SRP, DRY, KISS, meaningful naming).

## Current State

- **File**: `includes/services/class-cdw-ai-service.php`
- **Tests**: 31/31 passing
- **Main Issues**:
  1. SRP Violation: `execute_agentic_loop()` is 114 lines with too many responsibilities
  2. DRY Violation: Provider routing logic duplicated 3 times
  3. Code Duplication: Default usage array duplicated
  4. Poor Naming: `$tc` is unclear

## Confirmed Scope

- **Phases 1-5**: Execute now
- **Phase 6 (Dependency Injection)**: Deferred to later

## Execution Order

Execute each phase in order. After each phase, run tests to verify behavior is preserved.

```bash
cd /home/toni/Escritorio/Wordpress/wp-content/plugins/cdw
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

---

# PHASE 1: Add Tests First

## Objective

Add tests for `execute_agentic_loop()` to create a safety net before refactoring.

## File to Modify

`tests/php/unit/AiServiceTest.php`

## Changes Required

Add the following test methods to the `AiServiceTest` class (before the closing brace):

```php
// -----------------------------------------------------------------------
// execute_agentic_loop()
// -----------------------------------------------------------------------

public function test_execute_agentic_loop_returns_content_from_provider(): void {
    // Mock WordPress functions
    Functions\when( 'get_user_meta' )->justReturn( '' );
    Functions\when( 'update_user_meta' )->justReturn( true );
    Functions\when( 'get_transient' )->justReturn( 0 );
    Functions\when( 'set_transient' )->justReturn( true );

    // Mock the provider call to return a simple response
    Functions\when( 'wp_remote_post' )->alias( function( $url, $args ) {
        return array(
            'body'     => json_encode( array(
                'choices' => array(
                    array(
                        'message' => array(
                            'content' => 'Hello, I can help you manage your WordPress site.'
                        )
                    )
                ),
                'usage' => array(
                    'prompt_tokens'     => 10,
                    'completion_tokens' => 20,
                    'total_tokens'      => 30
                )
            )),
            'response' => array( 'code' => 200 )
        );
    });

    $result = \CDW_AI_Service::execute_agentic_loop(
        'Hello',
        array(),
        'test-api-key',
        'openai',
        'gpt-4o-mini',
        1,
        '',
        ''
    );

    $this->assertIsArray( $result );
    $this->assertArrayHasKey( 'content', $result );
    $this->assertStringContainsString( 'WordPress', $result['content'] );
    $this->assertArrayHasKey( 'usage', $result );
    $this->assertArrayHasKey( 'tool_calls_made', $result );
}

public function test_execute_agentic_loop_handles_tool_calls(): void {
    Functions\when( 'get_user_meta' )->justReturn( '' );
    Functions\when( 'update_user_meta' )->justReturn( true );
    Functions\when( 'get_transient' )->justReturn( 0 );
    Functions\when( 'set_transient' )->justReturn( true );
    Functions\when( 'sanitize_text_field' )->returnArg();
    Functions\when( 'sanitize_textarea_field' )->returnArg();

    // First call returns a tool call, second returns final response
    $call_count = 0;
    Functions\when( 'wp_remote_post' )->alias( function( $url, $args ) use ( &$call_count ) {
        $call_count++;
        
        if ( 1 === $call_count ) {
            // First call: provider wants to call a tool
            return array(
                'body' => json_encode( array(
                    'choices' => array(
                        array(
                            'message' => array(
                                'tool_calls' => array(
                                    array(
                                        'id'       => 'call_123',
                                        'type'     => 'function',
                                        'function' => array(
                                            'name'      => 'plugin_list',
                                            'arguments' => '{}'
                                        )
                                    )
                                )
                            )
                        )
                    ),
                    'usage' => array(
                        'prompt_tokens'     => 10,
                        'completion_tokens' => 5,
                        'total_tokens'      => 15
                    )
                )),
                'response' => array( 'code' => 200 )
            );
        } else {
            // Second call: final response after tool execution
            return array(
                'body' => json_encode( array(
                    'choices' => array(
                        array(
                            'message' => array(
                                'content' => 'Here is the list of plugins: akismet, hello-dolly'
                            )
                        )
                    ),
                    'usage' => array(
                        'prompt_tokens'     => 20,
                        'completion_tokens' => 30,
                        'total_tokens'      => 50
                    )
                )),
                'response' => array( 'code' => 200 )
            );
        }
    });

    // Mock plugin list command execution
    Functions\when( 'get_plugins' )->justReturn( array(
        'akismet/akismet.php' => array( 'Name' => 'Akismet', 'Version' => '5.0' ),
        'hello.php' => array( 'Name' => 'Hello Dolly', 'Version' => '1.7.2' )
    ) );
    Functions\when( 'is_plugin_active' )->justReturn( true );
    Functions\when( 'get_option' )->justReturn( array() );

    $result = \CDW_AI_Service::execute_agentic_loop(
        'List my plugins',
        array(),
        'test-api-key',
        'openai',
        'gpt-4o-mini',
        1,
        '',
        ''
    );

    $this->assertIsArray( $result );
    $this->assertArrayHasKey( 'tool_calls_made', $result );
    $this->assertNotEmpty( $result['tool_calls_made'] );
    $this->assertSame( 'plugin_list', $result['tool_calls_made'][0]['name'] );
}

public function test_execute_agentic_loop_returns_wp_error_on_api_failure(): void {
    Functions\when( 'get_user_meta' )->justReturn( '' );
    Functions\when( 'update_user_meta' )->justReturn( true );
    Functions\when( 'get_transient' )->justReturn( 0 );
    Functions\when( 'set_transient' )->justReturn( true );
    Functions\when( 'sanitize_text_field' )->returnArg();
    Functions\when( 'sanitize_textarea_field' )->returnArg();

    Functions\when( 'wp_remote_post' )->alias( function( $url, $args ) {
        return array(
            'body'     => json_encode( array(
                'error' => array( 'message' => 'Invalid API key' )
            )),
            'response' => array( 'code' => 401 )
        );
    });

    $result = \CDW_AI_Service::execute_agentic_loop(
        'Hello',
        array(),
        'invalid-key',
        'openai',
        'gpt-4o-mini',
        1,
        '',
        ''
    );

    $this->assertInstanceOf( \WP_Error::class, $result );
}
```

## Verification

After adding tests, run:
```bash
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

Expected: All 34 tests pass (31 existing + 3 new).

---

# PHASE 2: Add Class Constants

## Objective

Eliminate code duplication for the default usage array.

## File to Modify

`includes/services/class-cdw-ai-service.php`

## Changes Required

Add this constant after line 70 (after `RATE_LIMIT_WINDOW`):

```php
	/**
	 * Default token usage structure.
	 *
	 * @var array<string,int>
	 */
	const DEFAULT_USAGE = array(
		'prompt_tokens'     => 0,
		'completion_tokens' => 0,
		'total_tokens'      => 0,
		'request_count'     => 0,
	);
```

## Update Existing Code

### Replace lines 275-280 (in `get_user_ai_settings()`):

**Before:**
```php
$default_usage = array(
    'prompt_tokens'     => 0,
    'completion_tokens' => 0,
    'total_tokens'      => 0,
    'request_count'     => 0,
);
```

**After:**
```php
$default_usage = self::DEFAULT_USAGE;
```

### Replace lines 444-450 (in `record_usage()`):

**Before:**
```php
$existing = get_user_meta( $user_id, self::USAGE_META_KEY, true );
if ( ! is_array( $existing ) ) {
    $existing = array(
        'prompt_tokens'     => 0,
        'completion_tokens' => 0,
        'total_tokens'      => 0,
        'request_count'    => 0,
    );
}
```

**After:**
```php
$existing = get_user_meta( $user_id, self::USAGE_META_KEY, true );
if ( ! is_array( $existing ) ) {
    $existing = self::DEFAULT_USAGE;
}
```

## Verification

Run tests to ensure nothing broke:
```bash
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

---

# PHASE 3: Extract Provider Routing (DRY)

## Objective

Eliminate the triplicated if-else pattern for calling different AI providers.

## File to Modify

`includes/services/class-cdw-ai-service.php`

## New Method to Add

Add this new method after line 515 (after `parse_http_response`):

```php
	/**
	 * Routes the chat request to the appropriate provider.
	 *
	 * @param array<int,array<string,mixed>> $messages       Chat messages.
	 * @param array<int,array<string,mixed>> $tools          Tool definitions.
	 * @param string                         $api_key        Provider API key.
	 * @param string                         $model          Model ID.
	 * @param string                         $system_prompt  System prompt (used for Anthropic).
	 * @param string                         $base_url       Custom base URL for OpenAI-compatible.
	 * @param string                         $provider       Provider slug.
	 * @return array<string,mixed>|WP_Error Parsed response or WP_Error.
	 */
	private static function call_provider( $messages, $tools, $api_key, $model, $system_prompt = '', $base_url = '', $provider = '' ) {
		if ( 'openai' === $provider || 'custom' === $provider ) {
			return self::call_openai( $messages, $tools, $api_key, $model, $base_url );
		}

		if ( 'anthropic' === $provider ) {
			return self::call_anthropic( $messages, $tools, $api_key, $model, $system_prompt );
		}

		return self::call_google( $messages, $tools, $api_key, $model );
	}
```

## Update Existing Code

### Update FIRST provider call (lines 598-604)

**Before:**
```php
if ( 'openai' === $provider || 'custom' === $provider ) {
    $api_response = self::call_openai( $messages, $tools, $api_key, $model, $base_url );
} elseif ( 'anthropic' === $provider ) {
    $api_response = self::call_anthropic( $messages, $tools, $api_key, $model, $system_prompt );
} else {
    $api_response = self::call_google( $messages, $tools, $api_key, $model );
}
```

**After:**
```php
$api_response = self::call_provider( $messages, $tools, $api_key, $model, $system_prompt, $base_url, $provider );
```

### Update SECOND provider call (lines 629-635)

**Before:**
```php
if ( 'openai' === $provider || 'custom' === $provider ) {
    $final_response = self::call_openai( $messages, $tools, $api_key, $model, $base_url );
} elseif ( 'anthropic' === $provider ) {
    $final_response = self::call_anthropic( $messages, $tools, $api_key, $model, $system_prompt );
} else {
    $final_response = self::call_google( $messages, $tools, $api_key, $model );
}
```

**After:**
```php
$final_response = self::call_provider( $messages, $tools, $api_key, $model, $system_prompt, $base_url, $provider );
```

### Update test_api_key() method (lines 698-704)

**Before:**
```php
if ( 'openai' === $provider || 'custom' === $provider ) {
    $result = self::call_openai( $minimal_messages, array(), $api_key, $model, $base_url );
} elseif ( 'anthropic' === $provider ) {
    $result = self::call_anthropic( $minimal_messages, array(), $api_key, $model );
} else {
    $result = self::call_google( $minimal_messages, array(), $api_key, $model );
}
```

**After:**
```php
$result = self::call_provider( $minimal_messages, array(), $api_key, $model, '', $base_url, $provider );
```

## Verification

```bash
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

---

# PHASE 4: Split execute_agentic_loop() (SRP)

## Objective

Break down the 114-line `execute_agentic_loop()` into smaller, focused methods.

## File to Modify

`includes/services/class-cdw-ai-service.php`

## New Methods to Add

Add these methods after the `call_provider()` method (around line 540):

### Method 1: build_messages_array()

```php
	/**
	 * Builds the messages array for the AI provider.
	 *
	 * @param string                         $system_prompt  System prompt.
	 * @param array<int,array<string,mixed>> $history        Previous conversation turns.
	 * @param string                         $user_message   Current user message.
	 * @param string                         $provider       Provider slug (affects format).
	 * @return array<int,array<string,mixed>> Messages array.
	 */
	private static function build_messages_array( $system_prompt, $history, $user_message, $provider ) {
		$messages = array();

		if ( 'anthropic' !== $provider ) {
			$messages[] = array(
				'role'    => 'system',
				'content' => $system_prompt,
			);
		}

		$capped_history = array_slice( $history, -20 );
		foreach ( $capped_history as $turn ) {
			if ( isset( $turn['role'], $turn['content'] ) ) {
				$messages[] = array(
					'role'    => sanitize_text_field( $turn['role'] ),
					'content' => sanitize_textarea_field( $turn['content'] ),
				);
			}
		}

		$messages[] = array(
			'role'    => 'user',
			'content' => sanitize_textarea_field( $user_message ),
		);

		return $messages;
	}
```

### Method 2: handle_tool_calls()

```php
	/**
	 * Executes tool calls from the AI response and updates messages.
	 *
	 * @param array<string,mixed>            $api_response  The AI response with tool_calls.
	 * @param string                         $provider      Provider slug.
	 * @param int                           $user_id       WordPress user ID.
	 * @param array<int,array<string,mixed>> &$messages    Messages array to append to.
	 * @return array<int,array<string,mixed>> Array of tool calls made.
	 */
	private static function handle_tool_calls( $api_response, $provider, $user_id, &$messages ) {
		$tool_calls_made = array();

		if ( empty( $api_response['tool_calls'] ) ) {
			return $tool_calls_made;
		}

		foreach ( $api_response['tool_calls'] as $tool_call ) {
			$tool_output = self::execute_tool_call( $tool_call['name'], $tool_call['arguments'], $user_id );

			$tool_calls_made[] = array(
				'name'      => $tool_call['name'],
				'arguments' => $tool_call['arguments'],
				'output'    => $tool_output,
			);

			$messages[] = self::format_assistant_tool_call_message( $provider, $api_response, $tool_call );
			$messages[] = self::format_tool_result_message( $provider, $tool_call, $tool_output );
		}

		return $tool_calls_made;
	}
```

### Method 3: process_response()

```php
	/**
	 * Processes the final AI response and handles edge cases.
	 *
	 * @param array<string,mixed>            $final_response  The final AI response.
	 * @param array<int,array<string,mixed>> $tool_calls_made Array of tool calls executed.
	 * @param array<string,int>              $total_usage     Current usage totals.
	 * @return array{content: string, usage: array<string,int>} Processed result.
	 */
	private static function process_response( $final_response, $tool_calls_made, $total_usage ) {
		$final_content = $final_response['content'];

		if ( isset( $final_response['usage'] ) ) {
			$total_usage['prompt_tokens']     += (int) ( $final_response['usage']['prompt_tokens'] ?? 0 );
			$total_usage['completion_tokens'] += (int) ( $final_response['usage']['completion_tokens'] ?? 0 );
			$total_usage['total_tokens']      += (int) ( $final_response['usage']['total_tokens'] ?? 0 );
		}

		if ( '' === trim( $final_content ) && ! empty( $tool_calls_made ) ) {
			$outputs       = array_map(
				function ( $tc ) {
					return $tc['output'];
				},
				$tool_calls_made
			);
			$final_content = implode( "\n\n", array_map( 'strval', $outputs ) );
		}

		return array(
			'content' => $final_content,
			'usage'   => $total_usage,
		);
	}
```

## Refactor execute_agentic_loop()

Replace the entire `execute_agentic_loop()` method (lines 563-676) with this refactored version:

```php
	public static function execute_agentic_loop( $user_message, $history, $api_key, $provider, $model, $user_id, $custom_prompt = '', $base_url = '' ) {
		$tools         = self::get_tool_definitions();
		$system_prompt = self::build_system_prompt( $custom_prompt );

		// Build messages array
		$messages = self::build_messages_array( $system_prompt, $history, $user_message, $provider );

		// First provider call
		$api_response = self::call_provider( $messages, $tools, $api_key, $model, $system_prompt, $base_url, $provider );

		if ( is_wp_error( $api_response ) ) {
			return $api_response;
		}

		$total_usage = isset( $api_response['usage'] ) ? $api_response['usage'] : self::DEFAULT_USAGE;

		// Handle tool calls
		$tool_calls_made = self::handle_tool_calls( $api_response, $provider, $user_id, $messages );

		// If tools were called, make second provider call
		if ( ! empty( $tool_calls_made ) ) {
			$final_response = self::call_provider( $messages, $tools, $api_key, $model, $system_prompt, $base_url, $provider );

			if ( is_wp_error( $final_response ) ) {
				return array(
					'content'         => $api_response['content'],
					'tool_calls_made' => $tool_calls_made,
					'usage'           => $total_usage,
				);
			}

			$processed = self::process_response( $final_response, $tool_calls_made, $total_usage );
			$final_content = $processed['content'];
			$total_usage   = $processed['usage'];
		} else {
			$final_content = $api_response['content'];
		}

		self::record_usage( $user_id, $total_usage );

		return array(
			'content'         => $final_content,
			'tool_calls_made' => $tool_calls_made,
			'usage'           => $total_usage,
		);
	}
```

## Verification

```bash
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

Expected: All tests pass.

---

# PHASE 5: Improve Naming

## Objective

Replace unclear variable names with more descriptive ones.

## File to Modify

`includes/services/class-cdw-ai-service.php`

## Changes Required

### In format_assistant_tool_call_message() (line ~525)

Change parameter name:

**Before:**
```php
private static function format_assistant_tool_call_message( $provider, $api_response, $tc ) {
```

**After:**
```php
private static function format_assistant_tool_call_message( $provider, $api_response, $tool_call ) {
```

### In format_tool_result_message() (line ~537)

Change parameter name:

**Before:**
```php
private static function format_tool_result_message( $provider, $tc, $tool_output ) {
```

**After:**
```php
private static function format_tool_result_message( $provider, $tool_call, $tool_output ) {
```

### Update internal references

In `format_tool_result_message()`, update the references from `$tc` to `$tool_call`:

```php
// Inside format_tool_result_message, change:
'tool_use_id' => $tc['id'],

// To:
'tool_use_id' => $tool_call['id'],
```

In `format_assistant_tool_call_message()`, update similarly.

## Update CDW_AI_Providers.php

The same naming issue exists in `class-cdw-ai-providers.php`. Update:

1. `format_assistant_tool_call_message()` - parameter `$tc` → `$tool_call`
2. `format_tool_result_message()` - parameter `$tc` → `$tool_call`
3. Update all internal references within those methods

## Verification

```bash
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

---

# FINAL VERIFICATION

After completing all phases, run the full test suite:

```bash
cd /home/toni/Escritorio/Wordpress/wp-content/plugins/cdw
vendor/bin/phpunit tests/php/unit/AiServiceTest.php
```

Expected: All tests pass.

---

# SUMMARY OF CHANGES

| Phase | File | Changes |
|-------|------|---------|
| 1 | `tests/php/unit/AiServiceTest.php` | Add 3 new tests |
| 2 | `class-cdw-ai-service.php` | Add `DEFAULT_USAGE` constant, update 2 locations |
| 3 | `class-cdw-ai-service.php` | Add `call_provider()`, update 3 call sites |
| 4 | `class-cdw-ai-service.php` | Add 3 new methods, refactor `execute_agentic_loop()` |
| 5 | `class-cdw-ai-service.php`, `class-cdw-ai-providers.php` | Rename `$tc` to `$tool_call` |

---

# NOTES FOR NEXT AGENT

1. **Always run tests after each phase** to catch regressions early
2. **If tests fail**, revert changes and investigate before continuing
3. **Phase 6 (Dependency Injection)** is deferred - do not attempt
4. **The calling code** (`class-cdw-ai-controller.php`) should work without changes since we're keeping the same public method signatures
5. **If you need to add more tests** for edge cases, add them in Phase 1 style
